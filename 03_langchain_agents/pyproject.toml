[project]
name = "03-langchain-agents"
version = "0.1.0"
description = "LangChain ReAct agent with tools — learning project"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3.0",
    "langchain-openai>=0.3.0",
    "langgraph>=0.2.0",
    "python-dotenv>=1.0.0",
]
