[project]
name = "02-openai-intro"
version = "0.1.0"
description = "Simple LangChain + OpenAI demo for learning"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3.0",
    "langchain-openai>=0.3.0",
    "python-dotenv>=1.0.0",
]
